import { getTargetDivision, selectTextPart, getDivisionType } from './navigation';

describe('navigation', () => {
  it('is even possible to run this test module', () => {
    expect(1 + 1).toBe(2);
  });
});
